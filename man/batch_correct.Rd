% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/preprocessing_methods.R
\name{batch_correct}
\alias{batch_correct}
\alias{batch_correct,SummarizedExperiment,character-method}
\title{Correct for Batch Effects}
\usage{
batch_correct(object, batch_variable, bio_variables = NULL)

\S4method{batch_correct}{SummarizedExperiment,character}(object, batch_variable, bio_variables = NULL)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object. The first assay will be used.}

\item{batch_variable}{A single character string specifying the column name
in \code{colData(object)} that contains the batch information.}

\item{bio_variables}{An optional character vector of column names in
\code{colData(object)} that correspond to biological variables of interest.
These effects will be preserved during the correction. If \code{NULL} (the default),
only an intercept is protected.}
}
\value{
A \code{SummarizedExperiment} object with a new assay named
\code{"corrected"} containing the batch-corrected data.
}
\description{
This function uses the \code{removeBatchEffect} method from the limma package to
adjust for known batch effects in the data.
}
\examples{
# --- Create a sample SummarizedExperiment object ---
set.seed(123)
counts <- matrix(rnorm(100 * 6, mean = 10, sd = 2), nrow = 100, ncol = 6)
colnames(counts) <- paste0("Sample", 1:6)

sample_info <- data.frame(
  row.names = colnames(counts),
  Condition = rep(c("A", "B"), each = 3),
  Batch = rep(c("Batch1", "Batch2"), times = 3)
)

se <- SummarizedExperiment::SummarizedExperiment(assays = list(log_intensities = counts),
                           colData = sample_info)

# --- Run batch correction, preserving the "Condition" variable ---
corrected_se <- batch_correct(se,
                              batch_variable = "Batch",
                              bio_variables = "Condition")

# --- Inspect the result ---
SummarizedExperiment::assayNames(corrected_se) # Now includes "corrected"

}
