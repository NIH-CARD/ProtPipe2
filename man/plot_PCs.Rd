% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/clustering.R
\name{plot_PCs}
\alias{plot_PCs}
\alias{plot_PCs,SummarizedExperiment-method}
\title{Plot Principal Component Analysis Results}
\usage{
plot_PCs(object, condition = NA, pc_x = "PC1", pc_y = "PC2")

\S4method{plot_PCs}{SummarizedExperiment}(object, condition = NA, pc_x = "PC1", pc_y = "PC2")
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object.}

\item{condition}{A character string specifying the column name in the
\code{condition} slot to use for coloring the points. If \code{NA} (the default),
it will attempt to guess groups from sample names.}

\item{pc_x}{A character string for the principal component to plot on the
x-axis (e.g., \code{"PC1"}). Defaults to \code{"PC1"}.}

\item{pc_y}{A character string for the principal component to plot on the
y-axis (e.g., \code{"PC2"}). Defaults to \code{"PC2"}.}
}
\value{
A \code{ggplot} object, which can be further customized or printed.
}
\description{
Generates a scatter plot of principal components to visualize the relationships
between samples. This function serves as a convenient wrapper that first calls
\code{calculate_pca} and then plots the results using \code{ggplot2}.
}
\details{
As this function calls \code{calculate_pca} internally, the data in the object
must be imputed first. It is also recommended to use log-transformed and
normalized data for the best results.
}
\section{Functions}{
\itemize{
\item \code{plot_PCs(SummarizedExperiment)}: Method for SummarizedExperiment objects.

}}
\examples{
# Create a sample SummarizedExperiment object with missing data
raw_data <- data.frame(
  Gene = c("GENEA", "GENEB", "GENEC", "GENED", "GENEE", "GENEF"),
  Control_1 = c(10, 11, 12, 13, 14, 15),
  Control_2 = c(10.5, 11.5, 12.5, NA, 14.5, 15.5),
  Treatment_1 = c(15, 16, 17, 18, 19, 20),
  Treatment_2 = c(15.5, 16.5, 17.5, 18.5, 19.5, 20.5)
)
cond_df <- data.frame(
   SampleID = c("Control_1", "Control_2", "Treatment_1", "Treatment_2"),
   group = c("Control", "Control", "Treatment", "Treatment")
)
se <- create_se(raw_data, sample_metadata = cond_df)

# Impute missing values before plotting
se_imputed <- impute(se, value = 13.5)

# Generate the plot of PC1 vs PC2
p1 <- plot_PCs(se_imputed, condition = "group")
if (interactive()) {
  print(p1)
}

# Generate a plot of PC1 vs PC3
p2 <- plot_PCs(se_imputed, condition = "group", pc_x = "PC1", pc_y = "PC3")
if (interactive()) {
  print(p2)
}

}
