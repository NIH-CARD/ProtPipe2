% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{get_PCs}
\alias{get_PCs}
\title{Calculate Principal Components Analysis (PCA)}
\usage{
get_PCs(object, condition = NA)
}
\arguments{
\item{object}{A \code{ProtData} object. The data should be imputed and ideally
normalized and log-transformed.}

\item{condition}{A character string specifying the column name in the
\code{condition} slot to use for grouping the samples in the output. If \code{NA}
(the default), it will attempt to guess groups by removing numeric suffixes
from sample names.}
}
\value{
A list with two elements:
\describe{
\item{\code{summary}}{A data.table with the standard deviation, proportion of
variance, and cumulative proportion for each component.}
\item{\code{components}}{A data frame with the first 5 PCA scores (PC1-PC5)
for each sample, along with their assigned condition.}
}
}
\description{
Performs a principal component analysis (PCA) on the samples within a
\code{ProtData} object. This is a common method for quality control and for
visualizing the relationships between samples.
}
\details{
This function expects data that has already been cleaned and imputed. Missing
values (\code{NA}) in the data will cause the PCA to fail. It is also highly
recommended to perform log-transformation and normalization before PCA.

The function will automatically remove proteins (rows) with no variation
across samples before running the analysis.
}
