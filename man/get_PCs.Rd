% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/clustering.R
\name{get_PCs}
\alias{get_PCs}
\alias{get_PCs,SummarizedExperiment-method}
\title{Calculate Principal Components Analysis (PCA)}
\usage{
get_PCs(object, condition = NA)

\S4method{get_PCs}{SummarizedExperiment}(object, condition = NA)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object. The first assay is used.}

\item{condition}{A character string specifying the column name in
\code{colData(object)} to use for grouping the samples in the output. If \code{NA}
(the default), groups are inferred from sample names by removing numeric suffixes.}
}
\value{
A list with two elements:
\describe{
\item{\code{summary}}{A data.table with the standard deviation, proportion of
variance, and cumulative proportion for each component.}
\item{\code{components}}{A data frame with the first 5 PCA scores (PC1-PC5)
for each sample, along with their assigned condition.}
}
}
\description{
Performs a principal component analysis (PCA) on the samples within a
\code{SummarizedExperiment} object.
}
\details{
This function expects data that has already been cleaned and imputed. Missing
values (\code{NA}) will cause the PCA to fail. It is also highly
recommended to perform log-transformation and normalization before PCA.
The function will automatically remove proteins (rows) with zero variance
across samples before running the analysis.
}
\section{Functions}{
\itemize{
\item \code{get_PCs(SummarizedExperiment)}: Method for SummarizedExperiment objects.

}}
\examples{
# --- Create a sample SummarizedExperiment object ---
set.seed(123)
counts <- matrix(rnorm(100 * 6, mean = 10, sd = 2), nrow = 100, ncol = 6)
colnames(counts) <- paste0("Sample_", rep(c("A","B"), each=3), "_", 1:3)
sample_info <- data.frame(
  row.names = colnames(counts),
  Group = rep(c("A", "B"), each = 3)
)
se <- SummarizedExperiment(assays = list(log_intensities = counts),
                           colData = sample_info)

# --- Run PCA, specifying the group variable ---
pca_results <- get_PCs(se, condition = "Group")
head(pca_results$components)

}
