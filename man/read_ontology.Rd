% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{read_ontology}
\alias{read_ontology}
\title{Read a Custom Ontology File}
\usage{
read_ontology(file)
}
\arguments{
\item{file}{Path to a \code{.gmt}, \code{.csv}, or \code{.tsv} ontology file.}
}
\value{
A named list with \code{term2gene} and \code{term2name}.
}
\description{
Reads a user-supplied ontology into the \code{TERM2GENE} and optional \code{TERM2NAME}
structures required by \code{clusterProfiler::enricher()} and
\code{clusterProfiler::GSEA()}.

Supported formats are:
\itemize{
\item GMT files, where each line is \code{term}, \code{name}, then one or more gene IDs
\item Delimited text files (\code{csv} or \code{tsv}) with either:
\itemize{
\item 2 columns: \code{term}, \code{gene}
\item 3 columns: \code{term}, \code{name}, \code{gene}
}
}

Gene identifiers in uploaded ontologies must match the identifiers used for
enrichment. In the ProtPipe Shiny app, this currently means Entrez gene IDs.
}
