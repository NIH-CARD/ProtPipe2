% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_sample.R
\name{remove_sample}
\alias{remove_sample}
\title{Remove Specific Samples from a ProtData Object}
\usage{
remove_sample(object, samples)
}
\arguments{
\item{object}{A \code{ProtData} object.}

\item{samples}{A character vector containing the exact names of the samples to
be removed.}
}
\value{
A \code{ProtData} object with the specified samples removed.
}
\description{
This function removes one or more specified samples from all relevant slots
of a \code{ProtData} object, including the quantitative data (\code{data} slot) and
the sample condition metadata (\code{condition} slot).
}
\examples{
# Create a sample ProtData object with four samples
raw_data <- data.frame(
  Gene = c("GENEA", "GENEB", "GENEC"),
  SampleA = c(10, 11, 12),
  SampleB = c(12, 13, 14),
  SampleC = c(11, 12, 13),
  SampleD = c(14, 15, 16)
)
pd_obj <- create_protdata(dat = raw_data)
cat("Sample names before removal:\n")
print(colnames(pd_obj@data))

# Remove two of the samples by name
filtered_obj <- remove_sample(pd_obj, samples = c("SampleA", "SampleC"))

cat("\nSample names after removal:\n")
print(colnames(filtered_obj@data))

}
