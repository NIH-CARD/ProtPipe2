% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/differential_expression.R
\name{do_anova}
\alias{do_anova}
\alias{do_anova,SummarizedExperiment-method}
\title{Perform ANOVA-style differential expression across multiple groups}
\usage{
do_anova(object, condition, covariates = NULL)

\S4method{do_anova}{SummarizedExperiment}(object, condition, covariates = NULL)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object containing protein intensities and metadata.}

\item{condition}{String: column name in \code{colData(object)} that holds group labels (>= 3 groups).}

\item{covariates}{Optional character vector: column names in \code{colData(object)} to use as covariates.}
}
\value{
A data frame with metadata, intensities, one log fold change column per
non-reference group (versus an arbitrary baseline group), the omnibus \code{F}
statistic, \code{P.Value}, and \code{adj.P.Val}.
}
\description{
This function takes a \code{SummarizedExperiment} object and uses group labels in
\code{colData(object)} to test, for each protein, whether mean abundance differs
across \emph{all} levels of \code{condition} at once. It uses limma's moderated F-test
(empirical Bayes-shrunk variance, as in \code{do_limma_binary}) rather than a
classical per-protein \code{aov()}, which is more robust when group sizes are
small. \code{condition} must have at least 3 groups; for exactly 2 groups, use
\code{do_limma_binary} instead.
}
\section{Functions}{
\itemize{
\item \code{do_anova(SummarizedExperiment)}: Method for SummarizedExperiment objects.

}}
